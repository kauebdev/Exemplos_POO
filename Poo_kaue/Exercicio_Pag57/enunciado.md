
# Faça uma classe em PHP 
que tenha (1) um atributo nome
esse nome deve ter um valor fixo dentro da classe, 2 (dois)
atributos numéricos, (1) uma função que recebe esses 2 (dois)
valores via parâmetro, some esses valores e guarde o resultado na
função usando a instrução return e (1) uma função chamada
get_Nome que guarde na função o nome atribuído dentro da
classe. Crie um objeto da classe acima para atribuir valores
numéricos aos atributos, processe os mesmos na função e mostre
o resultado do objeto soma dentro de um comando echo junto
com o nome fixo definido dentro da classe.
## Desafio Fazer o mesmo utilizando um formulário PHP